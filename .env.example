# Environment variables (optional overrides)

# Formspree contact form — defaults to the portfolio form in code if unset.
# Set this in Vercel → Settings → Environment Variables to use a different form.
NEXT_PUBLIC_CONTACT_FORM_ACTION=https://formspree.io/f/xojrpknn
